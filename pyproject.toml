[project]
name = "extraction-factures"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openpyxl>=3.1.5",
    "pydantic>=2.13.4",
    "pymupdf>=1.28.2",
    "reportlab>=5.0.1",
    "sqlalchemy>=2.0.52",
    "streamlit>=1.62.0",
]

[project.scripts]
extraction-factures = "extraction_factures:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
